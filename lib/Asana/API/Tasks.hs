{-
   Asana

   This is the interface for interacting with the [Asana Platform](https://developers.asana.com). Our API reference is generated from our [OpenAPI spec] (https://raw.githubusercontent.com/Asana/developer-docs/master/defs/asana_oas.yaml).

   OpenAPI Version: 3.0.0
   Asana API version: 1.0
   Generated by OpenAPI Generator (https://openapi-generator.tech)
-}

{-|
Module : Asana.API.Tasks
-}

{-# LANGUAGE FlexibleContexts #-}
{-# LANGUAGE FlexibleInstances #-}
{-# LANGUAGE MonoLocalBinds #-}
{-# LANGUAGE MultiParamTypeClasses #-}
{-# LANGUAGE OverloadedStrings #-}
{-# OPTIONS_GHC -fno-warn-name-shadowing -fno-warn-unused-binds -fno-warn-unused-imports #-}

module Asana.API.Tasks where

import Asana.Core
import Asana.MimeTypes
import Asana.Model as M

import qualified Data.Aeson as A
import qualified Data.ByteString as B
import qualified Data.ByteString.Lazy as BL
import qualified Data.Data as P (Typeable, TypeRep, typeOf, typeRep)
import qualified Data.Foldable as P
import qualified Data.Map as Map
import qualified Data.Maybe as P
import qualified Data.Proxy as P (Proxy(..))
import qualified Data.Set as Set
import qualified Data.String as P
import qualified Data.Text as T
import qualified Data.Text.Encoding as T
import qualified Data.Text.Lazy as TL
import qualified Data.Text.Lazy.Encoding as TL
import qualified Data.Time as TI
import qualified Network.HTTP.Client.MultipartFormData as NH
import qualified Network.HTTP.Media as ME
import qualified Network.HTTP.Types as NH
import qualified Web.FormUrlEncoded as WH
import qualified Web.HttpApiData as WH

import Data.Text (Text)
import GHC.Base ((<|>))

import Prelude ((==),(/=),($), (.),(<$>),(<*>),(>>=),Maybe(..),Bool(..),Char,Double,FilePath,Float,Int,Integer,String,fmap,undefined,mempty,maybe,pure,Monad,Applicative,Functor)
import qualified Prelude as P

-- * Operations


-- ** Tasks

-- *** addDependenciesForTask

-- | @POST \/tasks\/{task_gid}\/addDependencies@
-- 
-- Set dependencies for a task
-- 
-- Marks a set of tasks as dependencies of this task, if they are not already dependencies. *A task can have at most 15 dependencies*.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
addDependenciesForTask 
  :: (Consumes AddDependenciesForTask MimeJSON, MimeRender MimeJSON InlineObject40)
  => InlineObject40 -- ^ "inlineObject40"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest AddDependenciesForTask MimeJSON InlineResponse2001 MimeJSON
addDependenciesForTask inlineObject40 (TaskGid taskGid) =
  _mkRequest "POST" ["/tasks/",toPath taskGid,"/addDependencies"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject40

data AddDependenciesForTask 
instance HasBodyParam AddDependenciesForTask InlineObject40 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam AddDependenciesForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam AddDependenciesForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes AddDependenciesForTask MimeJSON

-- | @application/json@
instance Produces AddDependenciesForTask MimeJSON


-- *** addDependentsForTask

-- | @POST \/tasks\/{task_gid}\/addDependents@
-- 
-- Set dependents for a task
-- 
-- Marks a set of tasks as dependents of this task, if they are not already dependents. *A task can have at most 30 dependents*.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
addDependentsForTask 
  :: (Consumes AddDependentsForTask MimeJSON, MimeRender MimeJSON InlineObject42)
  => InlineObject42 -- ^ "inlineObject42"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest AddDependentsForTask MimeJSON InlineResponse20022 MimeJSON
addDependentsForTask inlineObject42 (TaskGid taskGid) =
  _mkRequest "POST" ["/tasks/",toPath taskGid,"/addDependents"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject42

data AddDependentsForTask 
instance HasBodyParam AddDependentsForTask InlineObject42 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam AddDependentsForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam AddDependentsForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes AddDependentsForTask MimeJSON

-- | @application/json@
instance Produces AddDependentsForTask MimeJSON


-- *** addFollowersForTask

-- | @POST \/tasks\/{task_gid}\/addFollowers@
-- 
-- Add followers to a task
-- 
-- Adds followers to a task. Returns an empty data block. Each task can be associated with zero or more followers in the system. Requests to add/remove followers, if successful, will return the complete updated task record, described above.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
addFollowersForTask 
  :: (Consumes AddFollowersForTask MimeJSON, MimeRender MimeJSON InlineObject48)
  => InlineObject48 -- ^ "inlineObject48"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest AddFollowersForTask MimeJSON InlineResponse2001 MimeJSON
addFollowersForTask inlineObject48 (TaskGid taskGid) =
  _mkRequest "POST" ["/tasks/",toPath taskGid,"/addFollowers"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject48

data AddFollowersForTask 
instance HasBodyParam AddFollowersForTask InlineObject48 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam AddFollowersForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam AddFollowersForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes AddFollowersForTask MimeJSON

-- | @application/json@
instance Produces AddFollowersForTask MimeJSON


-- *** addProjectForTask

-- | @POST \/tasks\/{task_gid}\/addProject@
-- 
-- Add a project to a task
-- 
-- Adds the task to the specified project, in the optional location specified. If no location arguments are given, the task will be added to the end of the project.  `addProject` can also be used to reorder a task within a project or section that already contains it.  At most one of `insert_before`, `insert_after`, or `section` should be specified. Inserting into a section in an non-order-dependent way can be done by specifying section, otherwise, to insert within a section in a particular place, specify `insert_before` or `insert_after` and a task within the section to anchor the position of this task.  Returns an empty data block.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
addProjectForTask 
  :: (Consumes AddProjectForTask MimeJSON, MimeRender MimeJSON InlineObject44)
  => InlineObject44 -- ^ "inlineObject44"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest AddProjectForTask MimeJSON InlineResponse2001 MimeJSON
addProjectForTask inlineObject44 (TaskGid taskGid) =
  _mkRequest "POST" ["/tasks/",toPath taskGid,"/addProject"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject44

data AddProjectForTask 
instance HasBodyParam AddProjectForTask InlineObject44 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam AddProjectForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam AddProjectForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes AddProjectForTask MimeJSON

-- | @application/json@
instance Produces AddProjectForTask MimeJSON


-- *** addTagForTask

-- | @POST \/tasks\/{task_gid}\/addTag@
-- 
-- Add a tag to a task
-- 
-- Adds a tag to a task. Returns an empty data block.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
addTagForTask 
  :: (Consumes AddTagForTask MimeJSON, MimeRender MimeJSON InlineObject46)
  => InlineObject46 -- ^ "inlineObject46"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest AddTagForTask MimeJSON InlineResponse2001 MimeJSON
addTagForTask inlineObject46 (TaskGid taskGid) =
  _mkRequest "POST" ["/tasks/",toPath taskGid,"/addTag"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject46

data AddTagForTask 
instance HasBodyParam AddTagForTask InlineObject46 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam AddTagForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam AddTagForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes AddTagForTask MimeJSON

-- | @application/json@
instance Produces AddTagForTask MimeJSON


-- *** createSubtaskForTask

-- | @POST \/tasks\/{task_gid}\/subtasks@
-- 
-- Create a subtask
-- 
-- Creates a new subtask and adds it to the parent task. Returns the full record for the newly created subtask.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
createSubtaskForTask 
  :: (Consumes CreateSubtaskForTask MimeJSON, MimeRender MimeJSON InlineObject38)
  => InlineObject38 -- ^ "inlineObject38"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest CreateSubtaskForTask MimeJSON InlineResponse2016 MimeJSON
createSubtaskForTask inlineObject38 (TaskGid taskGid) =
  _mkRequest "POST" ["/tasks/",toPath taskGid,"/subtasks"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject38

data CreateSubtaskForTask 
instance HasBodyParam CreateSubtaskForTask InlineObject38 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam CreateSubtaskForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam CreateSubtaskForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes CreateSubtaskForTask MimeJSON

-- | @application/json@
instance Produces CreateSubtaskForTask MimeJSON


-- *** createTask

-- | @POST \/tasks@
-- 
-- Create a task
-- 
-- Creating a new task is as easy as POSTing to the `/tasks` endpoint with a data block containing the fields you’d like to set on the task. Any unspecified fields will take on default values.  Every task is required to be created in a specific workspace, and this workspace cannot be changed once set. The workspace need not be set explicitly if you specify `projects` or a `parent` task instead.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
createTask 
  :: (Consumes CreateTask MimeJSON, MimeRender MimeJSON InlineObject35)
  => InlineObject35 -- ^ "inlineObject35"
  -> AsanaRequest CreateTask MimeJSON InlineResponse2016 MimeJSON
createTask inlineObject35 =
  _mkRequest "POST" ["/tasks"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject35

data CreateTask 
instance HasBodyParam CreateTask InlineObject35 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam CreateTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam CreateTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes CreateTask MimeJSON

-- | @application/json@
instance Produces CreateTask MimeJSON


-- *** deleteTask

-- | @DELETE \/tasks\/{task_gid}@
-- 
-- Delete a task
-- 
-- A specific, existing task can be deleted by making a DELETE request on the URL for that task. Deleted tasks go into the “trash” of the user making the delete request. Tasks can be recovered from the trash within a period of 30 days; afterward they are completely removed from the system.  Returns an empty data record.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
deleteTask 
  :: TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest DeleteTask MimeNoContent InlineResponse2001 MimeJSON
deleteTask (TaskGid taskGid) =
  _mkRequest "DELETE" ["/tasks/",toPath taskGid]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)

data DeleteTask  

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam DeleteTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam DeleteTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)
-- | @application/json@
instance Produces DeleteTask MimeJSON


-- *** duplicateTask

-- | @POST \/tasks\/{task_gid}\/duplicate@
-- 
-- Duplicate a task
-- 
-- Creates and returns a job that will asynchronously handle the duplication.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
duplicateTask 
  :: (Consumes DuplicateTask MimeJSON, MimeRender MimeJSON InlineObject37)
  => InlineObject37 -- ^ "inlineObject37"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest DuplicateTask MimeJSON InlineResponse2007 MimeJSON
duplicateTask inlineObject37 (TaskGid taskGid) =
  _mkRequest "POST" ["/tasks/",toPath taskGid,"/duplicate"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject37

data DuplicateTask 
instance HasBodyParam DuplicateTask InlineObject37 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam DuplicateTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam DuplicateTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes DuplicateTask MimeJSON

-- | @application/json@
instance Produces DuplicateTask MimeJSON


-- *** getDependenciesForTask

-- | @GET \/tasks\/{task_gid}\/dependencies@
-- 
-- Get dependencies from a task
-- 
-- Returns the compact representations of all of the dependencies of a task.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
getDependenciesForTask 
  :: TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest GetDependenciesForTask MimeNoContent InlineResponse20022 MimeJSON
getDependenciesForTask (TaskGid taskGid) =
  _mkRequest "GET" ["/tasks/",toPath taskGid,"/dependencies"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)

data GetDependenciesForTask  

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam GetDependenciesForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam GetDependenciesForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | /Optional Param/ "limit" - Results per page. The number of objects to return per page. The value must be between 1 and 100.
instance HasOptionalParam GetDependenciesForTask Limit where
  applyOptionalParam req (Limit xs) =
    req `setQuery` toQuery ("limit", Just xs)

-- | /Optional Param/ "offset" - Offset token. An offset to the next page returned by the API. A pagination request will return an offset token, which can be used as an input parameter to the next request. If an offset is not passed in, the API will return the first page of results. 'Note: You can only pass in an offset that was returned to you via a previously paginated request.'
instance HasOptionalParam GetDependenciesForTask Offset where
  applyOptionalParam req (Offset xs) =
    req `setQuery` toQuery ("offset", Just xs)
-- | @application/json@
instance Produces GetDependenciesForTask MimeJSON


-- *** getDependentsForTask

-- | @GET \/tasks\/{task_gid}\/dependents@
-- 
-- Get dependents from a task
-- 
-- Returns the compact representations of all of the dependents of a task.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
getDependentsForTask 
  :: TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest GetDependentsForTask MimeNoContent InlineResponse20022 MimeJSON
getDependentsForTask (TaskGid taskGid) =
  _mkRequest "GET" ["/tasks/",toPath taskGid,"/dependents"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)

data GetDependentsForTask  

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam GetDependentsForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam GetDependentsForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | /Optional Param/ "limit" - Results per page. The number of objects to return per page. The value must be between 1 and 100.
instance HasOptionalParam GetDependentsForTask Limit where
  applyOptionalParam req (Limit xs) =
    req `setQuery` toQuery ("limit", Just xs)

-- | /Optional Param/ "offset" - Offset token. An offset to the next page returned by the API. A pagination request will return an offset token, which can be used as an input parameter to the next request. If an offset is not passed in, the API will return the first page of results. 'Note: You can only pass in an offset that was returned to you via a previously paginated request.'
instance HasOptionalParam GetDependentsForTask Offset where
  applyOptionalParam req (Offset xs) =
    req `setQuery` toQuery ("offset", Just xs)
-- | @application/json@
instance Produces GetDependentsForTask MimeJSON


-- *** getSubtasksForTask

-- | @GET \/tasks\/{task_gid}\/subtasks@
-- 
-- Get subtasks from a task
-- 
-- Returns a compact representation of all of the subtasks of a task.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
getSubtasksForTask 
  :: TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest GetSubtasksForTask MimeNoContent InlineResponse20022 MimeJSON
getSubtasksForTask (TaskGid taskGid) =
  _mkRequest "GET" ["/tasks/",toPath taskGid,"/subtasks"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)

data GetSubtasksForTask  

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam GetSubtasksForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam GetSubtasksForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | /Optional Param/ "limit" - Results per page. The number of objects to return per page. The value must be between 1 and 100.
instance HasOptionalParam GetSubtasksForTask Limit where
  applyOptionalParam req (Limit xs) =
    req `setQuery` toQuery ("limit", Just xs)

-- | /Optional Param/ "offset" - Offset token. An offset to the next page returned by the API. A pagination request will return an offset token, which can be used as an input parameter to the next request. If an offset is not passed in, the API will return the first page of results. 'Note: You can only pass in an offset that was returned to you via a previously paginated request.'
instance HasOptionalParam GetSubtasksForTask Offset where
  applyOptionalParam req (Offset xs) =
    req `setQuery` toQuery ("offset", Just xs)
-- | @application/json@
instance Produces GetSubtasksForTask MimeJSON


-- *** getTask

-- | @GET \/tasks\/{task_gid}@
-- 
-- Get a task
-- 
-- Returns the complete task record for a single task.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
getTask 
  :: TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest GetTask MimeNoContent InlineResponse2016 MimeJSON
getTask (TaskGid taskGid) =
  _mkRequest "GET" ["/tasks/",toPath taskGid]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)

data GetTask  

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam GetTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam GetTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)
-- | @application/json@
instance Produces GetTask MimeJSON


-- *** getTasks

-- | @GET \/tasks@
-- 
-- Get multiple tasks
-- 
-- Returns the compact task records for some filtered set of tasks. Use one or more of the parameters provided to filter the tasks returned. You must specify a `project` or `tag` if you do not specify `assignee` and `workspace`.  For more complex task retrieval, use [workspaces/{workspace_gid}/tasks/search](#search-tasks-in-a-workspace).
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
getTasks 
  :: AsanaRequest GetTasks MimeNoContent InlineResponse20022 MimeJSON
getTasks =
  _mkRequest "GET" ["/tasks"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)

data GetTasks  

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam GetTasks OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam GetTasks OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | /Optional Param/ "limit" - Results per page. The number of objects to return per page. The value must be between 1 and 100.
instance HasOptionalParam GetTasks Limit where
  applyOptionalParam req (Limit xs) =
    req `setQuery` toQuery ("limit", Just xs)

-- | /Optional Param/ "offset" - Offset token. An offset to the next page returned by the API. A pagination request will return an offset token, which can be used as an input parameter to the next request. If an offset is not passed in, the API will return the first page of results. 'Note: You can only pass in an offset that was returned to you via a previously paginated request.'
instance HasOptionalParam GetTasks Offset where
  applyOptionalParam req (Offset xs) =
    req `setQuery` toQuery ("offset", Just xs)

-- | /Optional Param/ "assignee" - The assignee to filter tasks on. *Note: If you specify `assignee`, you must also specify the `workspace` to filter on.*
instance HasOptionalParam GetTasks Assignee where
  applyOptionalParam req (Assignee xs) =
    req `setQuery` toQuery ("assignee", Just xs)

-- | /Optional Param/ "project" - The project to filter tasks on.
instance HasOptionalParam GetTasks Project where
  applyOptionalParam req (Project xs) =
    req `setQuery` toQuery ("project", Just xs)

-- | /Optional Param/ "section" - The section to filter tasks on. *Note: Currently, this is only supported in board views.*
instance HasOptionalParam GetTasks Section where
  applyOptionalParam req (Section xs) =
    req `setQuery` toQuery ("section", Just xs)

-- | /Optional Param/ "workspace" - The workspace to filter tasks on. *Note: If you specify `workspace`, you must also specify the `assignee` to filter on.*
instance HasOptionalParam GetTasks Workspace where
  applyOptionalParam req (Workspace xs) =
    req `setQuery` toQuery ("workspace", Just xs)

-- | /Optional Param/ "completed_since" - Only return tasks that are either incomplete or that have been completed since this time.
instance HasOptionalParam GetTasks CompletedSince where
  applyOptionalParam req (CompletedSince xs) =
    req `setQuery` toQuery ("completed_since", Just xs)

-- | /Optional Param/ "modified_since" - Only return tasks that have been modified since the given time.  *Note: A task is considered “modified” if any of its properties change, or associations between it and other objects are modified (e.g.  a task being added to a project). A task is not considered modified just because another object it is associated with (e.g. a subtask) is modified. Actions that count as modifying the task include assigning, renaming, completing, and adding stories.*
instance HasOptionalParam GetTasks ModifiedSince where
  applyOptionalParam req (ModifiedSince xs) =
    req `setQuery` toQuery ("modified_since", Just xs)
-- | @application/json@
instance Produces GetTasks MimeJSON


-- *** getTasksForProject

-- | @GET \/projects\/{project_gid}\/tasks@
-- 
-- Get tasks from a project
-- 
-- Returns the compact task records for all tasks within the given project, ordered by their priority within the project. Tasks can exist in more than one project at a time.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
getTasksForProject 
  :: ProjectGid -- ^ "projectGid" -  Globally unique identifier for the project.
  -> AsanaRequest GetTasksForProject MimeNoContent InlineResponse20022 MimeJSON
getTasksForProject (ProjectGid projectGid) =
  _mkRequest "GET" ["/projects/",toPath projectGid,"/tasks"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)

data GetTasksForProject  

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam GetTasksForProject OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam GetTasksForProject OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | /Optional Param/ "limit" - Results per page. The number of objects to return per page. The value must be between 1 and 100.
instance HasOptionalParam GetTasksForProject Limit where
  applyOptionalParam req (Limit xs) =
    req `setQuery` toQuery ("limit", Just xs)

-- | /Optional Param/ "offset" - Offset token. An offset to the next page returned by the API. A pagination request will return an offset token, which can be used as an input parameter to the next request. If an offset is not passed in, the API will return the first page of results. 'Note: You can only pass in an offset that was returned to you via a previously paginated request.'
instance HasOptionalParam GetTasksForProject Offset where
  applyOptionalParam req (Offset xs) =
    req `setQuery` toQuery ("offset", Just xs)
-- | @application/json@
instance Produces GetTasksForProject MimeJSON


-- *** getTasksForSection

-- | @GET \/sections\/{section_gid}\/tasks@
-- 
-- Get tasks from a section
-- 
-- *Board view only*: Returns the compact section records for all tasks within the given section.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
getTasksForSection 
  :: SectionGid -- ^ "sectionGid" -  The globally unique identifier for the section.
  -> AsanaRequest GetTasksForSection MimeNoContent InlineResponse20022 MimeJSON
getTasksForSection (SectionGid sectionGid) =
  _mkRequest "GET" ["/sections/",toPath sectionGid,"/tasks"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)

data GetTasksForSection  

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam GetTasksForSection OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam GetTasksForSection OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | /Optional Param/ "limit" - Results per page. The number of objects to return per page. The value must be between 1 and 100.
instance HasOptionalParam GetTasksForSection Limit where
  applyOptionalParam req (Limit xs) =
    req `setQuery` toQuery ("limit", Just xs)

-- | /Optional Param/ "offset" - Offset token. An offset to the next page returned by the API. A pagination request will return an offset token, which can be used as an input parameter to the next request. If an offset is not passed in, the API will return the first page of results. 'Note: You can only pass in an offset that was returned to you via a previously paginated request.'
instance HasOptionalParam GetTasksForSection Offset where
  applyOptionalParam req (Offset xs) =
    req `setQuery` toQuery ("offset", Just xs)
-- | @application/json@
instance Produces GetTasksForSection MimeJSON


-- *** getTasksForTag

-- | @GET \/tags\/{tag_gid}\/tasks@
-- 
-- Get tasks from a tag
-- 
-- Returns the compact task records for all tasks with the given tag. Tasks can have more than one tag at a time.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
getTasksForTag 
  :: TagGid -- ^ "tagGid" -  Globally unique identifier for the tag.
  -> AsanaRequest GetTasksForTag MimeNoContent InlineResponse20022 MimeJSON
getTasksForTag (TagGid tagGid) =
  _mkRequest "GET" ["/tags/",toPath tagGid,"/tasks"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)

data GetTasksForTag  

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam GetTasksForTag OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam GetTasksForTag OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | /Optional Param/ "limit" - Results per page. The number of objects to return per page. The value must be between 1 and 100.
instance HasOptionalParam GetTasksForTag Limit where
  applyOptionalParam req (Limit xs) =
    req `setQuery` toQuery ("limit", Just xs)

-- | /Optional Param/ "offset" - Offset token. An offset to the next page returned by the API. A pagination request will return an offset token, which can be used as an input parameter to the next request. If an offset is not passed in, the API will return the first page of results. 'Note: You can only pass in an offset that was returned to you via a previously paginated request.'
instance HasOptionalParam GetTasksForTag Offset where
  applyOptionalParam req (Offset xs) =
    req `setQuery` toQuery ("offset", Just xs)
-- | @application/json@
instance Produces GetTasksForTag MimeJSON


-- *** getTasksForUserTaskList

-- | @GET \/user_task_lists\/{user_task_list_gid}\/tasks@
-- 
-- Get tasks from a user task list
-- 
-- Returns the compact list of tasks in a user’s My Tasks list. The returned tasks will be in order within each assignee status group of `Inbox`, `Today`, and `Upcoming`. *Note: tasks in `Later` have a different ordering in the Asana web app than the other assignee status groups; this endpoint will still return them in list order in `Later` (differently than they show up in Asana, but the same order as in Asana’s mobile apps).* *Note: Access control is enforced for this endpoint as with all Asana API endpoints, meaning a user’s private tasks will be filtered out if the API-authenticated user does not have access to them.* *Note: Both complete and incomplete tasks are returned by default unless they are filtered out (for example, setting `completed_since=now` will return only incomplete tasks, which is the default view for “My Tasks” in Asana.)*
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
getTasksForUserTaskList 
  :: UserTaskListGid -- ^ "userTaskListGid" -  Globally unique identifier for the user task list.
  -> AsanaRequest GetTasksForUserTaskList MimeNoContent InlineResponse20022 MimeJSON
getTasksForUserTaskList (UserTaskListGid userTaskListGid) =
  _mkRequest "GET" ["/user_task_lists/",toPath userTaskListGid,"/tasks"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)

data GetTasksForUserTaskList  

-- | /Optional Param/ "completed_since" - Only return tasks that are either incomplete or that have been completed since this time. Accepts a date-time string or the keyword *now*. 
instance HasOptionalParam GetTasksForUserTaskList CompletedSinceText where
  applyOptionalParam req (CompletedSinceText xs) =
    req `setQuery` toQuery ("completed_since", Just xs)

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam GetTasksForUserTaskList OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam GetTasksForUserTaskList OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | /Optional Param/ "limit" - Results per page. The number of objects to return per page. The value must be between 1 and 100.
instance HasOptionalParam GetTasksForUserTaskList Limit where
  applyOptionalParam req (Limit xs) =
    req `setQuery` toQuery ("limit", Just xs)

-- | /Optional Param/ "offset" - Offset token. An offset to the next page returned by the API. A pagination request will return an offset token, which can be used as an input parameter to the next request. If an offset is not passed in, the API will return the first page of results. 'Note: You can only pass in an offset that was returned to you via a previously paginated request.'
instance HasOptionalParam GetTasksForUserTaskList Offset where
  applyOptionalParam req (Offset xs) =
    req `setQuery` toQuery ("offset", Just xs)
-- | @application/json@
instance Produces GetTasksForUserTaskList MimeJSON


-- *** removeDependenciesForTask

-- | @POST \/tasks\/{task_gid}\/removeDependencies@
-- 
-- Unlink dependencies from a task
-- 
-- Unlinks a set of dependencies from this task.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
removeDependenciesForTask 
  :: (Consumes RemoveDependenciesForTask MimeJSON, MimeRender MimeJSON InlineObject41)
  => InlineObject41 -- ^ "inlineObject41"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest RemoveDependenciesForTask MimeJSON InlineResponse20023 MimeJSON
removeDependenciesForTask inlineObject41 (TaskGid taskGid) =
  _mkRequest "POST" ["/tasks/",toPath taskGid,"/removeDependencies"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject41

data RemoveDependenciesForTask 
instance HasBodyParam RemoveDependenciesForTask InlineObject41 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam RemoveDependenciesForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam RemoveDependenciesForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes RemoveDependenciesForTask MimeJSON

-- | @application/json@
instance Produces RemoveDependenciesForTask MimeJSON


-- *** removeDependentsForTask

-- | @POST \/tasks\/{task_gid}\/removeDependents@
-- 
-- Unlink dependents from a task
-- 
-- Unlinks a set of dependents from this task.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
removeDependentsForTask 
  :: (Consumes RemoveDependentsForTask MimeJSON, MimeRender MimeJSON InlineObject43)
  => InlineObject43 -- ^ "inlineObject43"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest RemoveDependentsForTask MimeJSON InlineResponse20023 MimeJSON
removeDependentsForTask inlineObject43 (TaskGid taskGid) =
  _mkRequest "POST" ["/tasks/",toPath taskGid,"/removeDependents"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject43

data RemoveDependentsForTask 
instance HasBodyParam RemoveDependentsForTask InlineObject43 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam RemoveDependentsForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam RemoveDependentsForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes RemoveDependentsForTask MimeJSON

-- | @application/json@
instance Produces RemoveDependentsForTask MimeJSON


-- *** removeFollowerForTask

-- | @POST \/tasks\/{task_gid}\/removeFollowers@
-- 
-- Remove followers from a task
-- 
-- Removes each of the specified followers from the task if they are following. Returns the complete, updated record for the affected task.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
removeFollowerForTask 
  :: (Consumes RemoveFollowerForTask MimeJSON, MimeRender MimeJSON InlineObject49)
  => InlineObject49 -- ^ "inlineObject49"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest RemoveFollowerForTask MimeJSON InlineResponse2001 MimeJSON
removeFollowerForTask inlineObject49 (TaskGid taskGid) =
  _mkRequest "POST" ["/tasks/",toPath taskGid,"/removeFollowers"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject49

data RemoveFollowerForTask 
instance HasBodyParam RemoveFollowerForTask InlineObject49 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam RemoveFollowerForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam RemoveFollowerForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes RemoveFollowerForTask MimeJSON

-- | @application/json@
instance Produces RemoveFollowerForTask MimeJSON


-- *** removeProjectForTask

-- | @POST \/tasks\/{task_gid}\/removeProject@
-- 
-- Remove a project from a task
-- 
-- Removes the task from the specified project. The task will still exist in the system, but it will not be in the project anymore.  Returns an empty data block.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
removeProjectForTask 
  :: (Consumes RemoveProjectForTask MimeJSON, MimeRender MimeJSON InlineObject45)
  => InlineObject45 -- ^ "inlineObject45"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest RemoveProjectForTask MimeJSON InlineResponse2001 MimeJSON
removeProjectForTask inlineObject45 (TaskGid taskGid) =
  _mkRequest "POST" ["/tasks/",toPath taskGid,"/removeProject"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject45

data RemoveProjectForTask 
instance HasBodyParam RemoveProjectForTask InlineObject45 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam RemoveProjectForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam RemoveProjectForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes RemoveProjectForTask MimeJSON

-- | @application/json@
instance Produces RemoveProjectForTask MimeJSON


-- *** removeTagForTask

-- | @POST \/tasks\/{task_gid}\/removeTag@
-- 
-- Remove a tag from a task
-- 
-- Removes a tag from a task. Returns an empty data block.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
removeTagForTask 
  :: (Consumes RemoveTagForTask MimeJSON, MimeRender MimeJSON InlineObject47)
  => InlineObject47 -- ^ "inlineObject47"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest RemoveTagForTask MimeJSON InlineResponse2001 MimeJSON
removeTagForTask inlineObject47 (TaskGid taskGid) =
  _mkRequest "POST" ["/tasks/",toPath taskGid,"/removeTag"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject47

data RemoveTagForTask 
instance HasBodyParam RemoveTagForTask InlineObject47 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam RemoveTagForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam RemoveTagForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes RemoveTagForTask MimeJSON

-- | @application/json@
instance Produces RemoveTagForTask MimeJSON


-- *** searchTasksForWorkspace

-- | @GET \/workspaces\/{workspace_gid}\/tasks\/search@
-- 
-- Search tasks in a workspace
-- 
-- To mirror the functionality of the Asana web app's advanced search feature, the Asana API has a task search endpoint that allows you to build complex filters to find and retrieve the exact data you need. #### Premium access Like the Asana web product's advance search feature, this search endpoint will only be available to premium Asana users. A user is premium if any of the following is true:  - The workspace in which the search is being performed is a premium workspace - The user is a member of a premium team inside the workspace  Even if a user is only a member of a premium team inside a non-premium workspace, search will allow them to find data anywhere in the workspace, not just inside the premium team. Making a search request using credentials of a non-premium user will result in a `402 Payment Required` error. #### Pagination Search results are not stable; repeating the same query multiple times may return the data in a different order, even if the data do not change. Because of this, the traditional [pagination](https://developers.asana.com/docs/#pagination) available elsewhere in the Asana API is not available here. However, you can paginate manually by sorting the search results by their creation time and then modifying each subsequent query to exclude data you have already seen. Page sizes are limited to a maximum of 100 items, and can be specified by the `limit` query parameter. #### Eventual consistency Changes in Asana (regardless of whether they’re made though the web product or the API) are forwarded to our search infrastructure to be indexed. This process can take between 10 and 60 seconds to complete under normal operation, and longer during some production incidents. Making a change to a task that would alter its presence in a particular search query will not be reflected immediately. This is also true of the advanced search feature in the web product. #### Rate limits You may receive a `429 Too Many Requests` response if you hit any of our [rate limits](https://developers.asana.com/docs/#rate-limits). #### Custom field parameters | Parameter name | Custom field type | Accepted type | |---|---|---| | custom_fields.{gid}.is_set | All | Boolean | | custom_fields.{gid}.value | Text | String | | custom_fields.{gid}.value | Number | Number | | custom_fields.{gid}.value | Enum | Enum option ID | | custom_fields.{gid}.starts_with | Text only | String | | custom_fields.{gid}.ends_with | Text only | String | | custom_fields.{gid}.contains | Text only | String | | custom_fields.{gid}.less_than | Number only | Number | | custom_fields.{gid}.greater_than | Number only | Number |   For example, if the gid of the custom field is 12345, these query parameter to find tasks where it is set would be `custom_fields.12345.is_set=true`. To match an exact value for an enum custom field, use the gid of the desired enum option and not the name of the enum option: `custom_fields.12345.value=67890`.  Searching for multiple exact matches of a custom field is not supported.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
searchTasksForWorkspace 
  :: WorkspaceGid -- ^ "workspaceGid" -  Globally unique identifier for the workspace or organization.
  -> AsanaRequest SearchTasksForWorkspace MimeNoContent InlineResponse20022 MimeJSON
searchTasksForWorkspace (WorkspaceGid workspaceGid) =
  _mkRequest "GET" ["/workspaces/",toPath workspaceGid,"/tasks/search"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)

data SearchTasksForWorkspace  

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam SearchTasksForWorkspace OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam SearchTasksForWorkspace OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | /Optional Param/ "text" - Performs full-text search on both task name and description
instance HasOptionalParam SearchTasksForWorkspace ParamText where
  applyOptionalParam req (ParamText xs) =
    req `setQuery` toQuery ("text", Just xs)

-- | /Optional Param/ "resource_subtype" - Filters results by the task's resource_subtype
instance HasOptionalParam SearchTasksForWorkspace ResourceSubtype where
  applyOptionalParam req (ResourceSubtype xs) =
    req `setQuery` toQuery ("resource_subtype", Just xs)

-- | /Optional Param/ "assignee.any" - Comma-separated list of user identifiers
instance HasOptionalParam SearchTasksForWorkspace AssigneeAny where
  applyOptionalParam req (AssigneeAny xs) =
    req `setQuery` toQuery ("assignee.any", Just xs)

-- | /Optional Param/ "assignee.not" - Comma-separated list of user identifiers
instance HasOptionalParam SearchTasksForWorkspace AssigneeNot where
  applyOptionalParam req (AssigneeNot xs) =
    req `setQuery` toQuery ("assignee.not", Just xs)

-- | /Optional Param/ "assignee_status" - One of `inbox`, `today`, `upcoming`, or `later`
instance HasOptionalParam SearchTasksForWorkspace AssigneeStatus where
  applyOptionalParam req (AssigneeStatus xs) =
    req `setQuery` toQuery ("assignee_status", Just xs)

-- | /Optional Param/ "projects.any" - Comma-separated list of project IDs
instance HasOptionalParam SearchTasksForWorkspace ProjectsAny where
  applyOptionalParam req (ProjectsAny xs) =
    req `setQuery` toQuery ("projects.any", Just xs)

-- | /Optional Param/ "projects.not" - Comma-separated list of project IDs
instance HasOptionalParam SearchTasksForWorkspace ProjectsNot where
  applyOptionalParam req (ProjectsNot xs) =
    req `setQuery` toQuery ("projects.not", Just xs)

-- | /Optional Param/ "projects.all" - Comma-separated list of project IDs
instance HasOptionalParam SearchTasksForWorkspace ProjectsAll where
  applyOptionalParam req (ProjectsAll xs) =
    req `setQuery` toQuery ("projects.all", Just xs)

-- | /Optional Param/ "sections.any" - Comma-separated list of section or column IDs
instance HasOptionalParam SearchTasksForWorkspace SectionsAny where
  applyOptionalParam req (SectionsAny xs) =
    req `setQuery` toQuery ("sections.any", Just xs)

-- | /Optional Param/ "sections.not" - Comma-separated list of section or column IDs
instance HasOptionalParam SearchTasksForWorkspace SectionsNot where
  applyOptionalParam req (SectionsNot xs) =
    req `setQuery` toQuery ("sections.not", Just xs)

-- | /Optional Param/ "sections.all" - Comma-separated list of section or column IDs
instance HasOptionalParam SearchTasksForWorkspace SectionsAll where
  applyOptionalParam req (SectionsAll xs) =
    req `setQuery` toQuery ("sections.all", Just xs)

-- | /Optional Param/ "tags.any" - Comma-separated list of tag IDs
instance HasOptionalParam SearchTasksForWorkspace TagsAny where
  applyOptionalParam req (TagsAny xs) =
    req `setQuery` toQuery ("tags.any", Just xs)

-- | /Optional Param/ "tags.not" - Comma-separated list of tag IDs
instance HasOptionalParam SearchTasksForWorkspace TagsNot where
  applyOptionalParam req (TagsNot xs) =
    req `setQuery` toQuery ("tags.not", Just xs)

-- | /Optional Param/ "tags.all" - Comma-separated list of tag IDs
instance HasOptionalParam SearchTasksForWorkspace TagsAll where
  applyOptionalParam req (TagsAll xs) =
    req `setQuery` toQuery ("tags.all", Just xs)

-- | /Optional Param/ "teams.any" - Comma-separated list of team IDs
instance HasOptionalParam SearchTasksForWorkspace TeamsAny where
  applyOptionalParam req (TeamsAny xs) =
    req `setQuery` toQuery ("teams.any", Just xs)

-- | /Optional Param/ "followers.any" - Comma-separated list of user identifiers
instance HasOptionalParam SearchTasksForWorkspace FollowersAny where
  applyOptionalParam req (FollowersAny xs) =
    req `setQuery` toQuery ("followers.any", Just xs)

-- | /Optional Param/ "followers.not" - Comma-separated list of user identifiers
instance HasOptionalParam SearchTasksForWorkspace FollowersNot where
  applyOptionalParam req (FollowersNot xs) =
    req `setQuery` toQuery ("followers.not", Just xs)

-- | /Optional Param/ "created_by.any" - Comma-separated list of user identifiers
instance HasOptionalParam SearchTasksForWorkspace CreatedByAny where
  applyOptionalParam req (CreatedByAny xs) =
    req `setQuery` toQuery ("created_by.any", Just xs)

-- | /Optional Param/ "created_by.not" - Comma-separated list of user identifiers
instance HasOptionalParam SearchTasksForWorkspace CreatedByNot where
  applyOptionalParam req (CreatedByNot xs) =
    req `setQuery` toQuery ("created_by.not", Just xs)

-- | /Optional Param/ "assigned_by.any" - Comma-separated list of user identifiers
instance HasOptionalParam SearchTasksForWorkspace AssignedByAny where
  applyOptionalParam req (AssignedByAny xs) =
    req `setQuery` toQuery ("assigned_by.any", Just xs)

-- | /Optional Param/ "assigned_by.not" - Comma-separated list of user identifiers
instance HasOptionalParam SearchTasksForWorkspace AssignedByNot where
  applyOptionalParam req (AssignedByNot xs) =
    req `setQuery` toQuery ("assigned_by.not", Just xs)

-- | /Optional Param/ "liked_by.any" - Comma-separated list of user identifiers
instance HasOptionalParam SearchTasksForWorkspace LikedByAny where
  applyOptionalParam req (LikedByAny xs) =
    req `setQuery` toQuery ("liked_by.any", Just xs)

-- | /Optional Param/ "liked_by.not" - Comma-separated list of user identifiers
instance HasOptionalParam SearchTasksForWorkspace LikedByNot where
  applyOptionalParam req (LikedByNot xs) =
    req `setQuery` toQuery ("liked_by.not", Just xs)

-- | /Optional Param/ "commented_on_by.any" - Comma-separated list of user identifiers
instance HasOptionalParam SearchTasksForWorkspace CommentedOnByAny where
  applyOptionalParam req (CommentedOnByAny xs) =
    req `setQuery` toQuery ("commented_on_by.any", Just xs)

-- | /Optional Param/ "commented_on_by.not" - Comma-separated list of user identifiers
instance HasOptionalParam SearchTasksForWorkspace CommentedOnByNot where
  applyOptionalParam req (CommentedOnByNot xs) =
    req `setQuery` toQuery ("commented_on_by.not", Just xs)

-- | /Optional Param/ "due_on.before" - ISO 8601 date string
instance HasOptionalParam SearchTasksForWorkspace DueOnBefore where
  applyOptionalParam req (DueOnBefore xs) =
    req `setQuery` toQuery ("due_on.before", Just xs)

-- | /Optional Param/ "due_on.after" - ISO 8601 date string
instance HasOptionalParam SearchTasksForWorkspace DueOnAfter where
  applyOptionalParam req (DueOnAfter xs) =
    req `setQuery` toQuery ("due_on.after", Just xs)

-- | /Optional Param/ "due_on" - ISO 8601 date string or `null`
instance HasOptionalParam SearchTasksForWorkspace DueOn where
  applyOptionalParam req (DueOn xs) =
    req `setQuery` toQuery ("due_on", Just xs)

-- | /Optional Param/ "due_at.before" - ISO 8601 datetime string
instance HasOptionalParam SearchTasksForWorkspace DueAtBefore where
  applyOptionalParam req (DueAtBefore xs) =
    req `setQuery` toQuery ("due_at.before", Just xs)

-- | /Optional Param/ "due_at.after" - ISO 8601 datetime string
instance HasOptionalParam SearchTasksForWorkspace DueAtAfter where
  applyOptionalParam req (DueAtAfter xs) =
    req `setQuery` toQuery ("due_at.after", Just xs)

-- | /Optional Param/ "start_on.before" - ISO 8601 date string
instance HasOptionalParam SearchTasksForWorkspace StartOnBefore where
  applyOptionalParam req (StartOnBefore xs) =
    req `setQuery` toQuery ("start_on.before", Just xs)

-- | /Optional Param/ "start_on.after" - ISO 8601 date string
instance HasOptionalParam SearchTasksForWorkspace StartOnAfter where
  applyOptionalParam req (StartOnAfter xs) =
    req `setQuery` toQuery ("start_on.after", Just xs)

-- | /Optional Param/ "start_on" - ISO 8601 date string or `null`
instance HasOptionalParam SearchTasksForWorkspace StartOn where
  applyOptionalParam req (StartOn xs) =
    req `setQuery` toQuery ("start_on", Just xs)

-- | /Optional Param/ "created_on.before" - ISO 8601 date string
instance HasOptionalParam SearchTasksForWorkspace CreatedOnBefore where
  applyOptionalParam req (CreatedOnBefore xs) =
    req `setQuery` toQuery ("created_on.before", Just xs)

-- | /Optional Param/ "created_on.after" - ISO 8601 date string
instance HasOptionalParam SearchTasksForWorkspace CreatedOnAfter where
  applyOptionalParam req (CreatedOnAfter xs) =
    req `setQuery` toQuery ("created_on.after", Just xs)

-- | /Optional Param/ "created_on" - ISO 8601 date string or `null`
instance HasOptionalParam SearchTasksForWorkspace CreatedOn where
  applyOptionalParam req (CreatedOn xs) =
    req `setQuery` toQuery ("created_on", Just xs)

-- | /Optional Param/ "created_at.before" - ISO 8601 datetime string
instance HasOptionalParam SearchTasksForWorkspace CreatedAtBefore where
  applyOptionalParam req (CreatedAtBefore xs) =
    req `setQuery` toQuery ("created_at.before", Just xs)

-- | /Optional Param/ "created_at.after" - ISO 8601 datetime string
instance HasOptionalParam SearchTasksForWorkspace CreatedAtAfter where
  applyOptionalParam req (CreatedAtAfter xs) =
    req `setQuery` toQuery ("created_at.after", Just xs)

-- | /Optional Param/ "completed_on.before" - ISO 8601 date string
instance HasOptionalParam SearchTasksForWorkspace CompletedOnBefore where
  applyOptionalParam req (CompletedOnBefore xs) =
    req `setQuery` toQuery ("completed_on.before", Just xs)

-- | /Optional Param/ "completed_on.after" - ISO 8601 date string
instance HasOptionalParam SearchTasksForWorkspace CompletedOnAfter where
  applyOptionalParam req (CompletedOnAfter xs) =
    req `setQuery` toQuery ("completed_on.after", Just xs)

-- | /Optional Param/ "completed_on" - ISO 8601 date string or `null`
instance HasOptionalParam SearchTasksForWorkspace CompletedOn where
  applyOptionalParam req (CompletedOn xs) =
    req `setQuery` toQuery ("completed_on", Just xs)

-- | /Optional Param/ "completed_at.before" - ISO 8601 datetime string
instance HasOptionalParam SearchTasksForWorkspace CompletedAtBefore where
  applyOptionalParam req (CompletedAtBefore xs) =
    req `setQuery` toQuery ("completed_at.before", Just xs)

-- | /Optional Param/ "completed_at.after" - ISO 8601 datetime string
instance HasOptionalParam SearchTasksForWorkspace CompletedAtAfter where
  applyOptionalParam req (CompletedAtAfter xs) =
    req `setQuery` toQuery ("completed_at.after", Just xs)

-- | /Optional Param/ "modified_on.before" - ISO 8601 date string
instance HasOptionalParam SearchTasksForWorkspace ModifiedOnBefore where
  applyOptionalParam req (ModifiedOnBefore xs) =
    req `setQuery` toQuery ("modified_on.before", Just xs)

-- | /Optional Param/ "modified_on.after" - ISO 8601 date string
instance HasOptionalParam SearchTasksForWorkspace ModifiedOnAfter where
  applyOptionalParam req (ModifiedOnAfter xs) =
    req `setQuery` toQuery ("modified_on.after", Just xs)

-- | /Optional Param/ "modified_on" - ISO 8601 date string or `null`
instance HasOptionalParam SearchTasksForWorkspace ModifiedOn where
  applyOptionalParam req (ModifiedOn xs) =
    req `setQuery` toQuery ("modified_on", Just xs)

-- | /Optional Param/ "modified_at.before" - ISO 8601 datetime string
instance HasOptionalParam SearchTasksForWorkspace ModifiedAtBefore where
  applyOptionalParam req (ModifiedAtBefore xs) =
    req `setQuery` toQuery ("modified_at.before", Just xs)

-- | /Optional Param/ "modified_at.after" - ISO 8601 datetime string
instance HasOptionalParam SearchTasksForWorkspace ModifiedAtAfter where
  applyOptionalParam req (ModifiedAtAfter xs) =
    req `setQuery` toQuery ("modified_at.after", Just xs)

-- | /Optional Param/ "is_blocking" - Filter to incomplete tasks with dependents
instance HasOptionalParam SearchTasksForWorkspace IsBlocking where
  applyOptionalParam req (IsBlocking xs) =
    req `setQuery` toQuery ("is_blocking", Just xs)

-- | /Optional Param/ "is_blocked" - Filter to tasks with incomplete dependencies
instance HasOptionalParam SearchTasksForWorkspace IsBlocked where
  applyOptionalParam req (IsBlocked xs) =
    req `setQuery` toQuery ("is_blocked", Just xs)

-- | /Optional Param/ "has_attachment" - Filter to tasks with attachments
instance HasOptionalParam SearchTasksForWorkspace HasAttachment where
  applyOptionalParam req (HasAttachment xs) =
    req `setQuery` toQuery ("has_attachment", Just xs)

-- | /Optional Param/ "completed" - Filter to completed tasks
instance HasOptionalParam SearchTasksForWorkspace Completed where
  applyOptionalParam req (Completed xs) =
    req `setQuery` toQuery ("completed", Just xs)

-- | /Optional Param/ "is_subtask" - Filter to subtasks
instance HasOptionalParam SearchTasksForWorkspace IsSubtask where
  applyOptionalParam req (IsSubtask xs) =
    req `setQuery` toQuery ("is_subtask", Just xs)

-- | /Optional Param/ "sort_by" - One of `due_date`, `created_at`, `completed_at`, `likes`, or `modified_at`, defaults to `modified_at`
instance HasOptionalParam SearchTasksForWorkspace SortBy where
  applyOptionalParam req (SortBy xs) =
    req `setQuery` toQuery ("sort_by", Just xs)

-- | /Optional Param/ "sort_ascending" - Default `false`
instance HasOptionalParam SearchTasksForWorkspace SortAscending where
  applyOptionalParam req (SortAscending xs) =
    req `setQuery` toQuery ("sort_ascending", Just xs)
-- | @application/json@
instance Produces SearchTasksForWorkspace MimeJSON


-- *** setParentForTask

-- | @POST \/tasks\/{task_gid}\/setParent@
-- 
-- Set the parent of a task
-- 
-- parent, or no parent task at all. Returns an empty data block. When using `insert_before` and `insert_after`, at most one of those two options can be specified, and they must already be subtasks of the parent.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
setParentForTask 
  :: (Consumes SetParentForTask MimeJSON, MimeRender MimeJSON InlineObject39)
  => InlineObject39 -- ^ "inlineObject39"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest SetParentForTask MimeJSON InlineResponse2016 MimeJSON
setParentForTask inlineObject39 (TaskGid taskGid) =
  _mkRequest "POST" ["/tasks/",toPath taskGid,"/setParent"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject39

data SetParentForTask 
instance HasBodyParam SetParentForTask InlineObject39 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam SetParentForTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam SetParentForTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes SetParentForTask MimeJSON

-- | @application/json@
instance Produces SetParentForTask MimeJSON


-- *** updateTask

-- | @PUT \/tasks\/{task_gid}@
-- 
-- Update a task
-- 
-- A specific, existing task can be updated by making a PUT request on the URL for that task. Only the fields provided in the `data` block will be updated; any unspecified fields will remain unchanged.  When using this method, it is best to specify only those fields you wish to change, or else you may overwrite changes made by another user since you last retrieved the task.  Returns the complete updated task record.
-- 
-- AuthMethod: 'AuthOAuthOauth2', 'AuthBasicPersonalAccessToken'
-- 
updateTask 
  :: (Consumes UpdateTask MimeJSON, MimeRender MimeJSON InlineObject36)
  => InlineObject36 -- ^ "inlineObject36"
  -> TaskGid -- ^ "taskGid" -  The task to operate on.
  -> AsanaRequest UpdateTask MimeJSON InlineResponse2016 MimeJSON
updateTask inlineObject36 (TaskGid taskGid) =
  _mkRequest "PUT" ["/tasks/",toPath taskGid]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOauth2)
    `_hasAuthType` (P.Proxy :: P.Proxy AuthBasicPersonalAccessToken)
    `setBodyParam` inlineObject36

data UpdateTask 
instance HasBodyParam UpdateTask InlineObject36 

-- | /Optional Param/ "opt_pretty" - Provides “pretty” output. Provides the response in a “pretty” format. In the case of JSON this means doing proper line breaking and indentation to make it readable. This will take extra time and increase the response size so it is advisable only to use this during debugging.
instance HasOptionalParam UpdateTask OptPretty where
  applyOptionalParam req (OptPretty xs) =
    req `setQuery` toQuery ("opt_pretty", Just xs)

-- | /Optional Param/ "opt_fields" - Defines fields to return. Some requests return *compact* representations of objects in order to conserve resources and complete the request more efficiently. Other times requests return more information than you may need. This option allows you to list the exact set of fields that the API should be sure to return for the objects. The field names should be provided as paths, described below. The id of included objects will always be returned, regardless of the field options.
instance HasOptionalParam UpdateTask OptFields where
  applyOptionalParam req (OptFields xs) =
    req `setQuery` toQueryColl CommaSeparated ("opt_fields", Just xs)

-- | @application/json@
instance Consumes UpdateTask MimeJSON

-- | @application/json@
instance Produces UpdateTask MimeJSON

